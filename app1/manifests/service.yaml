apiVersion: v1
kind: Service
metadata:
  name: nginx-app1-service
  namespace: default
  labels:
    app: nginx-app1
spec:
  selector:
    app: nginx-app1
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP